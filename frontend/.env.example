# API base hostname. Empty = Vite proxies /api to localhost in dev only.
VITE_API_URL=

# Root path when hosting on GitHub Pages project sites (usually /<repo>/). Leave unset for /. 
# CI sets this automatically unless you define repository variable VITE_BASE_PATH.
# VITE_BASE_PATH=
